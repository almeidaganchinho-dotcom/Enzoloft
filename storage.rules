rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // Galeria: leitura pública, escrita só para admins autenticados
    match /gallery/{fileName} {
      allow read: if true;
      allow write: if request.auth != null;
    }
  }
}
